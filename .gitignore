# Private/sensitive configuration files
.zshrc.local
.gitconfig.local
**/private/
**/.env
**/.env.local
# The real one lives at ~/.config/sing-box/secrets.env, outside this repo — but
# secrets.env.example sits next to the proxy config and invites a copy here,
# and `**/.env` does not match this name. This repo is public.
secrets.env

# macOS
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
# subagent-driven-development scratch (ledger, briefs, review packages)
.superpowers/
# local design/plan docs — not published
docs/superpowers/

# Claude Code writes machine-specific state here (skillOverrides, autoMode
# environment notes) — not portable, and not for a public repo.
claude/settings.json
